export class Status {
    constructor(
        public readonly id_user: number,
        public readonly manual: boolean,
        public readonly temperature: number,
        public readonly activemanual: boolean

    ) {}

}
